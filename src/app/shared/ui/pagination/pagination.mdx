import { Controls, Meta, Story, Title } from '@storybook/blocks';

import * as PaginationStories from './pagination.stories';

<Meta of={PaginationStories} />

<Title of={PaginationStories} />


# Description

This is a pagination component that allows the user to navigate through a list of items.

---

# Behavior

The component displays a list of pages that the user can navigate through. The user can click on a page number to navigate to that page.

<Story of={PaginationStories.Default} />

When it has few pages, it displays all of them.

<Story of={PaginationStories.WithFewItems} />

When it has many pages, it displays a subset of themng add @angular-eslint/schematics.

---

# Inputs

<Controls />
